spring.config.import=optional:file:.env[.properties]

spring.application.name=shop
server.port=${SERVER_PORT:8080}

# ===== Database (SQL Server) =====
spring.datasource.url=jdbc:mysql://localhost:3306/shop?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Tr1@MySQL2026

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update

# ===== Thymeleaf =====
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

# ===== Mail (SMTP) =====
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=shootingspiralsonic@gmail.com
spring.mail.password=ejuw cwpb yjhs qxgu
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===== Cloudinary =====
cloudinary.cloud-name=zugyvnuh
cloudinary.api-key=234844361353485
cloudinary.api-secret=_09n4d-Iov1Xbb2vodRKe43S7VY

# ===== Upload =====
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=20MB

# ===== Encoding =====
spring.servlet.encoding.enabled=true
spring.servlet.encoding.charset=UTF-8
spring.servlet.encoding.force=true
spring.servlet.encoding.force-request=true
spring.servlet.encoding.force-response=true

spring.main.allow-bean-definition-overriding=true

# ===== Seed data (see DataInitializer) =====
app.seed.admin-username=${SEED_ADMIN_USERNAME:admin}
app.seed.admin-password=${SEED_ADMIN_PASSWORD:admin123}
app.seed.admin-email=${SEED_ADMIN_EMAIL:admin@example.com}